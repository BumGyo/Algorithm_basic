* 방금 만든 노드 클래스를 사용하는 트리 클래스를 정의하기
class MyTreeClass:
   # 필수 요소 : 루트 노드의 주소를 보관할 멤버 변수가 필요
   # -> 초기화 함수를 정의한 다음에 root_ptr 멤버 변수를 생성
   def  __init__(self):
      print("새로운 트리 객체를 메모리에 생성합니다!!")
      self.root_ptr = None
      print("새로운 인스턴스 변수인 루트를 만들었습니다.")

   # 현재 트리 자료 구조에 새로운 노드를 추가해 주는 함수
   """

   """

