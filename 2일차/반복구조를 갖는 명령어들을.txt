* 반복구조를 갖는 명령어들을
  재귀호출 방법을 사용해서 명령어를 적게 작성
  -> 단축 명령어

* 재귀호출 방법을 사용해서 함수를 만들기
  -> 함수의 기본 구조
def  함수이름( 매개 변수 ):
   # 매개 변수 : 함수 안에서 바깥으로
   # 탈출하기 위해서 사용하는 변수
   # if 명령어를 사용해서 종료 처리
   if 종료조건 :
      return 명령어를 작성
      # return 명령어 : 함수를 호출한 곳으로
      # 복귀












