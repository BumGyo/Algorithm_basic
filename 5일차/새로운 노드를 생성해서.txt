* 새로운 노드를 생성해서
  현재 헤드 포인터 변수가 
  가리키고 있는 노드의 주소로 연결하기

1) 새로운 노드를 메모리에 생성
가. 필요한 정보
    데이터 : 사용자로부터 입력 받도록 하겠습니다.
    print("노드에 저장할 데이터를 입력하세요: ",end=" ")
    input_data = int(input())
    # 새로운 변수(객체)를 선언 : MyNodeClass 클래스를 사용
    new_object = MyNodeClass(input_data)
    print("새로운 변수(객체) 만들기 완료")
    # 2번에서 받는 입력 값은 새로 만든 노드의 주소
    # 지역 변수가 갖고 있는 값을 다른 함수로 전달
    return new_object

2) 새로 만들어진 노드의 주소를 기억하고 있다가
   헤드 포인터 변수에 지정하기



